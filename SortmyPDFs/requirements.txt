# Core dependencies for SortmyPDFs (OneDrive/Graph ingest + OCR-based sorting)
# Install with:
#   cd SortmyPDFs
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

msal>=1.28.0
requests>=2.31.0
python-dotenv>=1.0.1
